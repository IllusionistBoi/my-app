# Optional. Leave unset to use Vite's local proxy and Vercel's same-origin /api proxy.
VITE_API_URL=/api

# A preview deployment should point at an isolated staging backend.
# VITE_API_URL=https://staging-api.example.com/api
